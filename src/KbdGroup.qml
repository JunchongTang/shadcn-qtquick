import QtQuick
import QtQuick.Layouts

// shadcn KbdGroup —— 键位分组容器(.cn-kbd-group:inline-flex items-center gap-1)。
// 子项(Kbd / 分隔文本 "+")横向排列、垂直居中(RowLayout 默认 AlignVCenter),间距 gap-1。
RowLayout {
    spacing: Theme.space1   // gap-1
}
