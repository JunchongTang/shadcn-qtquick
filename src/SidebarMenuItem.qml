import QtQuick
import QtQuick.Layouts

// shadcn SidebarMenuItem —— 单个菜单项的包装(<li>,relative)。
// 通常内含一个 SidebarMenuButton;简化版未实现 menu-action / menu-badge / menu-sub。
ColumnLayout {
    Layout.fillWidth: true
    spacing: 1
}
